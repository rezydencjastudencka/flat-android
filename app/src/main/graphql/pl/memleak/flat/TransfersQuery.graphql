query TransfersQuery($year: Int!, $month: Int!) {
    summary {
        monthly(year: $year, month: $month) {
            amount
            user {
                id
                username
            }
         }
    }

    transfers(year: $year, month: $month) {
        id
        amount
        date
        name
        fromUser {
            id
            username
        }
        toUser {
            id
            username
        }
    }
    me {
        id
    }
}