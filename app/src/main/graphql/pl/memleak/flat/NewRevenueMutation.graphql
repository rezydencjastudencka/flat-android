mutation NewRevenueMutation($amount: String!, $date: Date!, $name: String!, $to: [ID!], $category: ID) {
  addRevenue(amount: $amount, date: $date, name: $name, to: $to, category: $category) {
    ...RevenueFragment
  }
}
